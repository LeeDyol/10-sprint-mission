## 로그 관련 설정
#logging:
#  file:
#    name: .logs/discodeit.log                                       # .logs 경로 내 discodeit.log 파일 생성
#
#  # 로그 출력 패턴
#  pattern:
#    console: "%d{yy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
#    file: "%d{yy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
#
#  # 로그 관리 정책
#  logback:
#    rollingpolicy:
#      file-name-pattern: .logs/discodeit.%d{yyyy-MM-dd}.log         # 로그 파일 이름 : discodeit.2025-03-26.log 형식
#      max-history: 30                                               # 보관 기간 : 30일
#      total-size-cap: 5GB                                           # 최대 저장 용량 : 5GB
#      clean-history-on-start: true                                  # 애플리케이션 시작 시 오래된 로그 삭제